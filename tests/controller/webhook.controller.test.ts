import { WebhookController } from "../../src/class/base/base-controller.class";

describe("WebhookController export", () => {
  test("WebhookController should be defined", () => {
    expect(WebhookController).toBeDefined();
  });
});
