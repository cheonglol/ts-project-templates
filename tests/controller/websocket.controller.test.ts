import { WebsocketController } from "../../src/class/base/base-controller.class";

describe("WebsocketController export", () => {
  test("WebsocketController should be defined", () => {
    expect(WebsocketController).toBeDefined();
  });
});
